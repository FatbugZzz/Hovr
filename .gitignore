# ── Android & Java ───────────────────────────────────────────────────────────
*.iml
*.apk
*.aab
*.ap_
*.dex
*.class
bin/
gen/

# ── Build output (Archivos temporales de compilación) ─────────────────────────
build/
app/build/
.gradle/
.navigation/

# ── Android Studio / IntelliJ ─────────────────────────────────────────────────
.idea/
*.iws
*.ipr
out/
captures/
.externalNativeBuild/
.cxx/
*.swp
*~
.shelf/

# ── Local config (NUNCA subir — contiene rutas personales del SDK y claves) ───
local.properties
secrets.properties
gradle.properties
# Nota: Si usas gradle.properties para configurar la memoria de Gradle, 
# a veces se sube, pero si contiene tokens o llaves, mejor ignorarlo.

# ── Keystore & Signatures (CRÍTICO: Nunca subir tus llaves de firma) ──────────
*.jks
*.keystore
*.pk8
*.p12
*.pem
google-services.json

# ── Sistema Operativo ─────────────────────────────────────────────────────────
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log